#pragma once

#include "vivid.h"
#include "../switch.h"

///============================================================
/// 床に設置するタイプのスイッチクラス
/// ・キャラクターが踏むことで反応するスイッチ
/// ・サイズは固定値で管理
///============================================================
class CFloorSwitch
	: public ISwitch
{
public:
	//========================================================
	// コンストラクタ
	//========================================================
	CFloorSwitch(void);

	//========================================================
	// デストラクタ
	//========================================================
	~CFloorSwitch(void) = default;

	//========================================================
	// 描画処理
	// フロアスイッチのテクスチャを描画
	//========================================================
	void Draw(void) override;

private:
	//========================================================
	// 当たり判定サイズ（固定）
	//========================================================
	static const int		m_width;	// スイッチの幅
	static const int		m_height;	// スイッチの高さ
};
