#pragma once

#include "vivid.h"
#include "../item.h"

//==================================================
// ジャンプ強化アイテムクラス
//==================================================
// IItem を継承する取得型アイテム。
// 効果処理は基底クラス／管理側に委譲し、本クラスは描画のみを担当。
class CJumpUpItem
	: public IItem
{
public:
	//==================================================
	// コンストラクタ / デストラクタ
	//==================================================

	// 固定サイズ・当たり判定・ID は cpp 側で定義
	CJumpUpItem(void);

	// 明示的なリソースを保持しないため default
	~CJumpUpItem(void) = default;

	//==================================================
	// 描画
	//==================================================

	// IItem インターフェース実装
	void Draw(void) override;

private:
	//==================================================
	// クラス共通パラメータ
	//==================================================

	// 描画・当たり判定サイズ
	static const int   m_width;
	static const int   m_height;

	// 円形当たり判定用半径
	static const float m_radius;
};
